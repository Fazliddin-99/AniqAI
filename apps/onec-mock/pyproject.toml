[project]
name = "onec-mock"
version = "0.1.0"
description = "Мок HTTP-сервиса 1С (/hs/copilot/v1) для dev — заменяет расширение конфигурации"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "copilot-shared",
]

[tool.uv.sources]
copilot-shared = { path = "../../packages/shared", editable = true }

[tool.uv]
package = false
